import { Fragment } from "react";

export default (
	<Fragment>
	<textarea className="pier-input-textarea pier-input-textarea--md pier-input-textarea--dark pier-input-textarea--error" placeholder="Error"></textarea>
	</Fragment>
);
